# No keys are needed to develop the design or download reports.
# Set hosted secrets through Sites. Never put secrets in client code.
RESEND_API_KEY=
REPORT_FROM_EMAIL=
ENQUIRY_TO_EMAIL=
RETENTION_DAYS=90
# Use a strong generated secret for scheduled retention cleanup.
MAINTENANCE_TOKEN=

# Optional owner-issued search engine verification tokens.
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=
